#!/usr/bin/env bash
# Quick Command Reference Card
# Save as: run.sh (or run.ps1 for PowerShell)

# ============================================================================
# EMAIL CADENCE SYSTEM - QUICK COMMANDS
# ============================================================================

echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "  EMAIL CADENCE SYSTEM - Quick Reference"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""

echo "📋 STEP 1: Prerequisites"
echo "   ✓ Node.js 18+ installed"
echo "   ✓ Yarn installed"
echo "   ✓ Temporal server running:"
echo "       temporal server start-dev"
echo "   OR:"
echo "       docker run -d --name temporal -p 7233:7233 -p 8233:8233 temporalio/auto-setup:latest"
echo ""

echo "📋 STEP 2: Install Dependencies"
echo "   $ cd e:\\project\\email_project"
echo "   $ yarn install"
echo ""

echo "📋 STEP 3: Run All Services"
echo "   $ yarn dev"
echo ""
echo "   This starts:"
echo "   • Frontend:  http://localhost:3000"
echo "   • API:       http://localhost:3001"
echo "   • Worker:    Listening on task queue"
echo ""

echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "  INDIVIDUAL SERVICES"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""
echo "  yarn dev:web      # Frontend only (port 3000)"
echo "  yarn dev:api      # API only (port 3001)"
echo "  yarn dev:worker   # Worker only"
echo "  yarn build        # Build all"
echo ""

echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "  API ENDPOINTS"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""
echo "  Cadences:"
echo "    POST   /cadences              Create cadence"
echo "    GET    /cadences              List cadences"
echo "    GET    /cadences/:id          Get cadence"
echo "    PUT    /cadences/:id          Update cadence"
echo ""
echo "  Enrollments:"
echo "    POST   /enrollments           Start workflow"
echo "    GET    /enrollments/:id       Get status"
echo "    POST   /enrollments/:id/update-cadence    Update signal"
echo ""

echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "  DOCUMENTATION"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""
echo "  📖 README.md          → Full documentation (550+ lines)"
echo "  📖 QUICKSTART.md      → Quick reference"
echo "  📖 PROJECT.md         → Project overview"
echo "  📖 IMPLEMENTATION.md  → Technical details"
echo "  📖 DELIVERY.md        → Delivery summary (this section)"
echo ""

echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "  WORKFLOW EXAMPLE"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""
echo "  1. Create Cadence (UI or API)"
echo "     POST /cadences with cadence JSON"
echo ""
echo "  2. Enroll Contact"
echo "     POST /enrollments { cadenceId, contactEmail }"
echo ""
echo "  3. Monitor Progress"
echo "     GET /enrollments/:id"
echo "     Refresh to see currentStepIndex, status"
echo ""
echo "  4. Update Running Workflow (Optional)"
echo "     POST /enrollments/:id/update-cadence { steps }"
echo ""

echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""
echo "Ready? Run: yarn install && yarn dev"
echo ""
echo "Then open: http://localhost:3000"
echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
